package eStore;

public class Seller extends User{
    public Seller(String name, int age, String password, String emailAddress, Address homeAddress, String phoneNumber) {
        super(name, age, password, emailAddress, homeAddress, phoneNumber);
    }
}
