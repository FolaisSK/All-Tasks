package eStore;

public class Admin extends User{
    public Admin(String name, int age, String password, String emailAddress, Address homeAddress, String phoneNumber) {
        super(name, age, password, emailAddress, homeAddress, phoneNumber);
    }
}
